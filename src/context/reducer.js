export function reducer(state, action) {
  const addBooking = () => {};

  const removeBooking = () => {};

  switch (action.type) {
    case "add":

    case "remove":

    case "clear":

    default:
      return state;
  }
}
