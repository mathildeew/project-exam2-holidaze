import styled from "styled-components";

export const VenueContainer = styled.div`
  .fascilities {
    svg {
      color: var(--primary);
      font-size: 1.8rem;
      margin-right: 10px;
    }
  }
`;
