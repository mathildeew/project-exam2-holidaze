import { useState, useEffect } from "react";

export default function deleteD(url) {
  const [data, setData] = useState([]);

  useEffect(() => {
    async function deleteData() {
      try {
      } catch {}
      console.log(error);
    }
    deleteData();
  }, [url]);
}
